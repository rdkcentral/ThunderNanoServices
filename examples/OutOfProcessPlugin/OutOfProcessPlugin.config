set(startmode ${PLUGIN_OUTOFPROCESSPLUGIN_STARTMODE})
set(resumed ${PLUGIN_OUTOFPROCESSPLUGIN_RESUMED})

map()
    key(root)
    map()
        kv(mode ${PLUGIN_OUTOFPROCESSPLUGIN_MODE})
    end()
end()
ans(configuration)


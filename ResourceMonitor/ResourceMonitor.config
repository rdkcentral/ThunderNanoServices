set(startmode ${PLUGIN_RESOURCEMONITOR_STARTMODE})
set(resumed ${PLUGIN_RESOURCEMONITOR_RESUMED})

map()
    key(root)
    map()
        kv(mode ${PLUGIN_RESOURCEMONITOR_MODE})
    end()
end()
ans(configuration)

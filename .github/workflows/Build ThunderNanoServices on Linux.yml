name: Build ThunderNanoServices on Linux

permissions:
  contents: read

on:
  workflow_dispatch:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  Thunder:
    uses: rdkcentral/Thunder/.github/workflows/Linux build template.yml@master

  ThunderInterfaces:
    needs: Thunder
    uses: rdkcentral/ThunderInterfaces/.github/workflows/Linux build template.yml@development/increase_text_tag_coverage #tmp change

  ThunderNanoServices:
    needs: ThunderInterfaces
    uses: ./.github/workflows/Linux build template.yml

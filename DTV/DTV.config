set(autostart ${PLUGIN_DTV_AUTOSTART})
set(preconditions Platform)

map()
    key(root)
    map()
        kv(outofprocess ${PLUGIN_DTV_OUTOFPROCESS})
    end()
    kv(subtitleprocessing false)
    kv(teletextprocessing false)
end()
ans(configuration)
